# For sensitive string, use
# <env var> = "${SSM:<aws SSM parameter store key to read the value from>}"
# The value will be replaced by CI with the value read from the parameter store
NEXT_PUBLIC_API_BASE_URL="https://backend.huerray.de"
NEXT_PUBLIC_SANITY_PROJECT_ID="abcd1234"
NEXT_PUBLIC_SANITY_DATASET="abcd1234"
